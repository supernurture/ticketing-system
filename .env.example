# The key must already exist in configs/config.yaml, otherwise it is ignored.
APP_ENV=development # development, staging, or production

SERVER_MODE=debug # test, debug, or release

DATABASES_POSTGRES_TICKETING_USER=postgres
DATABASES_POSTGRES_TICKETING_PASSWORD=postgres

AUTH_JWT_SECRET=change-me-to-a-random-string-of-32-chars-or-more
